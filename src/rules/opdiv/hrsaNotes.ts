/**
 * Shared note appended to HRSA structural rule descriptions.
 * HRSA templates update frequently; a missing section may reflect a template
 * change rather than a writer error.
 */
export const HRSA_TEMPLATE_CAVEAT =
  'Note: HRSA templates are updated periodically. If your template does not include this section, verify against the most recent version before acting on this warning. This tool may not always reflect the latest HRSA template.';
